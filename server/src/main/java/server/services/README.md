# services

This contains the functions interacting with the database including:

- `GetItem`
  - Fetches an item from the database.
- `AddItem`
  - Adds items to the database.
- `GetItemTableLength`
  - Fetches the length of the Item table within the database.
