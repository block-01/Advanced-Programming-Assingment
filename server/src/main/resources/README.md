# Webapp frontend

This directory contains the assets for the Dashboards frontend with `templates` containing the HTML files and `static` that contains three subdirectories which are:

- `assets`
  - Contains image based assets for the frontend ui.
- `css`
  - Contains css used in the frontend ui.
- `scripts`
  - Contains the JavaScript files used to enable functionality in the frontend.
